'use strict';
describe('Given a Job Clip application', function () {
//    load the controller's module
    beforeEach(module('Job ClipApp'));

    it('the app should run', function () {
        expect(1).to.equal(1);
    });
});
